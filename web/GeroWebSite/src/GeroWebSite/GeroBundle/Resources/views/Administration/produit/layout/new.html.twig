{% extends '::layout/layoutAdmin.html.twig' %}

{% block body %}
<div class="container position-absolute" style="right: -4%; top: 13%;">
    <div class="row">
        <div class="col-12">
    <h1>Produit creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('adminProduit_index') }}">Back to the list</a>
        </li>
    </ul>
        </div>
    </div>
</div>
{% endblock %}
