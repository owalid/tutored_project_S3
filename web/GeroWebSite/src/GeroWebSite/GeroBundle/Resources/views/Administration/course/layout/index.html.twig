{% extends '::layout/layoutBenevole.html.twig' %}
{% block body %}
    <h1>Liste de course</h1>

    <table>
        <thead>
            <tr>

                <th>Montantcourse</th>
                <th>Datecourse</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for course in courses %}
            <tr>

                <td>{{ course.montantCourse }} €</td>
                <td>{% if course.dateCourse %}{{ course.dateCourse|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('benevole_course_show', { 'id': course.id }) }}"><i class="icon-book"></i> Voir</a>
                        </li>
                        <li>
                            <a href="{{ path('benevole_course_edit', { 'id': course.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('benevole_course_new') }}">Create a new course</a>
        </li>
    </ul>
{% endblock %}
