<div class="well">
    <ul class="nav nav-list">
        <li class="nav-header">Panier</li>
        <li>
           <a href="{{ path('Panier') }}">
               {% if article == 0 %}
                   (0) panier
               {% else %}
                    ({{ article }}) panier
               {% endif %}
           </a>
        </li>
    </ul>
</div>