{% extends '::layout/layoutAdmin.html.twig' %}
{% block body %}
    <h1>Page creation</h1>
            <div class="span10">
                {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <input type="submit" value="Ajouter" />
                {{ form_end(form) }}
            </div><br>
        <div class="span12">
        <ul class="list-group">
            <li class="list-group-flush">
                <a href="{{ path('adminPages_index') }}"><i class="icon-backward"></i> Retour</a>
            </li>
        </ul>
    </div>
{% endblock %}
